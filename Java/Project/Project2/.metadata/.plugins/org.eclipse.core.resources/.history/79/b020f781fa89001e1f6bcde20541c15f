package com.telusko.app.servive;

import java.util.List;

import com.telusko.app.model.Transaction;
import com.telusko.app.model.User;
import com.telusko.app.servive.impl.UserServiceInterface;

public class UserService implements UserServiceInterface
{
	TransactionService ts;
	List<User> user;
	public UserService(List<User> user) 
	{
		this.user=user;
	}

	@Override
	public boolean login(int id, String pwd) 
	{
		boolean bool=false;
		for(User u:user)
		{
			if(u.getId().equals(id))
			{
				bool=true;
				
				if(u.getPassword().equals(pwd))
				{
					System.out.println(u.getName()+" Total credit points : "+u.getPoints());
				}
				else
				{
					bool=false;
					
				}
					
			}
		}
		
		return bool;
		
	}

	@Override
	public void showCreditPoints(int id) 
	{
		User users=user.get(id);
		//ts.calculateCreditPoints(id, );
		//System.out.println("Total credit points earned by user "+id+" : "+users.getPoints());
	}

	@Override
	public void redemRewards() 
	{
			
	}

	@Override
	public boolean login(List<User> user, int id, String pwd) {
		// TODO Auto-generated method stub
		return false;
	}
	
}
